\documentclass[
	aspectratio=169, % 16:9 slides. Use 'aspectratio=43' for classic 4:3.
]{beamer}

\usetheme[nosectionpages]{scribe} % The scribe beamer theme (matches scribe.cls)
% Theme options:
%   \usetheme[noframenumbers]{scribe} % hide the frame counter
%   \usetheme[nosectionpages]{scribe} % no automatic section divider slides

\usepackage{svg}
\usepackage{lipsum} % For generating dummy text
\newcommand{\mylink}[2]{\href{#1}{\textsf{#2}}}

\lstdefinestyle{scribe}{
    basicstyle=\ttfamily\small,
    keywordstyle=\color{MidnightBlue}\bfseries,
    commentstyle=\color{black!55}\itshape,
    stringstyle=\color{ForestGreen!70!black},
	identifierstyle=\color{RawSienna},
    numberstyle=\tiny\color{black!45},
    showstringspaces=false,
    breaklines=true,
    captionpos=b,
    aboveskip=1em,
    belowskip=0.5em,
}
\lstset{style=scribe}

% Define different aliases (along with colors) for comments
\scribedefinecomment{fb}{orange}
\scribedefinecomment{jd}{blue}

% Uncomment the following line to hide comments in the final slides.
% \scribeshowcommentsfalse

\author{Federico Bruzzone \orcidlink{0000-0002-8701-8853}}
\institute{%
	Computer Science Department, Università degli Studi di Milano\\
	Supervisor: Prof. \textbf{Walter Cazzola} \\
	Email: \href{mailto:federico.bruzzone@unimi.it}{\texttt{federico.bruzzone@unimi.it}} \\
	Website: \mylink{https://federicobruzzone.github.io}{federicobruzzone.github.io}
}
\date{22 June 2026}

% Short versions used in the footline
\title[]{A Multi-Dimensional, Per-Pass Empirical Study of\\ the LLVM Optimization Pipeline}
\author[Federico Bruzzone]{Federico Bruzzone \orcidlink{0000-0002-8701-8853}}
\subtitle{Ph.D. Student's Activity Report}

\begin{document}

% ============================
% ========== Title ===========
% ============================
\begin{frame}[plain,noframenumbering]
	\titlepage
\end{frame}

% ============================
% ========== Outline =========
% ============================
% \begin{frame}{Outline}
% 	\tableofcontents
% \end{frame}

% ============================
% ========== Content =========
% ============================
\section{Introduction}

\begin{frame}[fragile]{Optimizing compilers}
	\begin{center}

	Since the dawn of computing, compiler optimizations have long bridged high-level abstractions and efficient machine code~\cite{Ershov58, Heller61, McKeeman65, Gear65, Lowry69, Busam69}.

	\textbf{Optimizing compilers}~\cite{Allen66, Wulf73b} use analysis passes to guide transformations~\cite{Cooper22, Kennedy01b, Paige97} that enhance performance without altering observable behavior~\cite{Bacon94, Dodds18, Aho06}---modern architectures (e.g., x86-64, Aarch64) make this an interplay of interdependent passes~\cite{Cooper22}.

	\textbf{LLVM}~\cite{Lattner04} exemplifies this complexity, serving as the standard backend for C/C++, Rust~\cite{Matsakis14}, Julia~\cite{Bezanson12}, and via \textbf{MLIR}~\cite{Lattner21}---for \textit{machine learning} (ML) and \textit{deep learning} (DL) stacks~\cite{Paszke19, Abadi16} that rely on ML/DL optimizing compilers~\cite{Li20b, Xing19, Zhao18, Liu22d} (e.g., NVCC\footnote{
		\mylink{https://developer.nvidia.com/cuda-llvm-compiler}{developer.nvidia.com/cuda-llvm-compiler}
	}, OpenXLA~\cite{Sabne20}, TVM~\cite{Chen18c}) and automotive pipelines.\footnote{
		Tesla Full Self-Driving v14.3: \mylink{https://stats.tessie.com/versions/2026.2.9.6}{stats.tessie.com/versions/2026.2.9.6}
	}

	\end{center}

	% \begin{itemize}
	% 	% \item Minimalist, content-first layout~\cite{Scribe25}
	% 	\item A thin accent rule under every frame title
	% 	\item Triangle bullets in the accent color
	% 	      \begin{itemize}
	% 		      \item Second-level items inherit the style
	% 	      \end{itemize}
	% \end{itemize}

	% \fbcomment{This is a comment in orange from Federico Bruzzone.}

	% \scribedivider

	% The \verb|\scribedivider| ornament works on slides too.
\end{frame}

\begin{frame}{LLVM Optimization Pipeline}
	LLVM is a modular SSA-based compilation framework~\cite{Rosen88, Alpern88, Cytron89}. Its \texttt{opt} tool applies sequences of analysis and transformation passes, forming the core optimization pipeline.


	\begin{center}
	\includegraphics[width=0.8\textwidth]{./images/LLVM-Passes-only.png}
	\end{center}

	It comprises parameterized nested managers (\texttt{{\color{MidnightBlue}{\textbf{module}}}, {\color{OliveGreen}{\textbf{function}}}, {\color{BrickRed}{\textbf{cgscc}}}, {\color{Mulberry}{\textbf{loop}}}, {\color{BlueGreen}{\textbf{loop-mssa}}}}), each scoped to a specific IR granularity.
\end{frame}

\begin{frame}{We need better optimization evaluation!}
	Per-pass impact analysis is critical for compiler engineers and end-users~\cite{Popescu25, Demertzi11, Cavazos07} to: 
	\begin{itemize}
    	\item[\dingblue{43}] address \textit{undecidable}~\cite{Ashouri17, Davidson07, Vegdahl82} phase ordering challenges~\cite{Touati06, Freudenberger92, Zhao25},
    	\item[\dingblue{43}] improve selection heuristics and ML-based auto-tuning~\cite{Pan25, Zhu24, Ashouri18, Tiwari09, Pan08, Pan06, Kulkarni04b},
    	\item[\dingblue{43}] guide new optimization design, and
    	\item[\dingblue{43}] understand pass--hardware interactions~\cite{Davidson07}.
  	\end{itemize}

	However, existing work evaluates LLVM optimizations in isolation or focuses mainly on execution time~\cite{DeLaTorre18}, overlooking multi-dimensional trade-offs---a pass improving runtime may increase binary size or hurt cache locality.

	Moreover, the debate over hand-written GPU kernels vs.\ ML-compiler-optimized kernels remains open~\cite{Palkar18}.\footnote{
  		\mylink{https://venturebeat.com/technology/octoml-optimizes-apache-tvm-for-apples-m1-beats-core-ml-4-by-29}{octoml-optimizes-apache-tvm-for-apples-m1-beats-core-ml-4-by-29}
	}
\end{frame}

\begin{frame}{A Study to Rule Them All {\small (150+ studies at ICSE'26 A*)}}
	We address this gap with a  multi-dimensional, systematic empirical study of the LLVM \texttt{-O3} pipeline, measuring not only \textbf{runtime} but also \textbf{compile time}, \textbf{binary size}, and a broad set of \textbf{HW performance counters}\footnote{
   		E.g., those exposed by Model-Specific Registers (MSRs) on x86 CPUs: cache misses, \texttt{cycles/instructions} for ILP, and energy consumption~\cite{VanKempen25, Lee24, Pereira17} via RAPL.
	} similar to the methodology of Chen \textit{et al.}~\cite{Chen12}.
	
    {\small
    \only<1>{ \begin{graybox}{} \color{gray}\textbf{RQ\textsubscript{1}.} \textit{What is the marginal performance impact of individual passes within the LLVM \texttt{-O3} optimization pipeline?} \end{graybox} }
    \only<2->{ \begin{infobox}{} \textbf{RQ\textsubscript{1}.} \textit{What is the marginal performance impact of individual passes within the LLVM \texttt{-O3} optimization pipeline?} \end{infobox} }

    \only<1-2>{ \begin{graybox}{} \color{gray}\textbf{RQ\textsubscript{2}.} \textit{To what extent do optimization passes trade off execution speed, compilation overhead, and binary footprint?} \end{graybox} }
    \only<3->{ \begin{infobox}{} \textbf{RQ\textsubscript{2}.} \textit{To what extent do optimization passes trade off execution speed, compilation overhead, and binary footprint?} \end{infobox} }

    \only<1-3>{ \begin{graybox}{} \color{gray}\textbf{RQ\textsubscript{3}.} \textit{How do IR-level transformations affect the evolution of HW performance counters across optimization pipelines?} \end{graybox} }
    \only<4->{ \begin{infobox}{} \textbf{RQ\textsubscript{3}.} \textit{How do IR-level transformations affect the evolution of HW performance counters across optimization pipelines?} \end{infobox} }

    \only<1-4>{ \begin{graybox}{} \color{gray}\textbf{RQ\textsubscript{4}.} \textit{What fraction of achievable speedup is lost to intra-pipeline interference, and which pass drives the loss?} \end{graybox} }
    \only<5->{ \begin{infobox}{} \textbf{RQ\textsubscript{4}.} \textit{What fraction of achievable speedup is lost to intra-pipeline interference, and which pass drives the loss?} \end{infobox} }
}
\end{frame}

\begin{frame}[fragile]{Experimental setup}
    All experiments ran on Intel Core i9-12900KF:
    \begin{itemize}
        \item[\dingblue{43}] Alder Lake (8P+8E cores, 5.2\,GHz max);
        \item[\dingblue{43}] 128\,GB RAM;
        \item[\dingblue{43}] Fedora Linux 43 (kernel 6.19);
        \item[\dingblue{43}] Cache hierarchy: 640\,KiB \textbf{L1-D}, 768\,KiB \textbf{L1-I}, 14\,MiB \textbf{L2}, 30\,MiB \textbf{L3}.
    \end{itemize}
    
    We used LLVM 21.1.8 compiled in release, with no assertions, \textit{profile-guided optimizations} (PGO) disabled, studying the \texttt{-O3} pipeline.
    
    Runtime uses \texttt{hyperfine} (3 warmup, 15 timed runs), counters use \texttt{perf stat} (2 warmup, 10 timed) in a separate batch---10 runs suffice for CI95 below 1\% on all counter metrics.
\end{frame}

% cp tesi-bruzzone/trunk/papers/icse27/submission/figs/polybench_figs/fig16e_cache_ipc_cpi_energy_ribbon_2x3.pdf ./llvm-phd-presentation/images
\begin{frame}[fragile]{Cumulative speedup, normalised gain, and top-20 passes}
    \begin{columns}[T]
	\begin{column}{0.5\textwidth}
           \begin{center}
	       \includegraphics[width=\textwidth, trim={0.25cm 0.25cm 0.25cm 0.25cm}, clip]{./images/figP9_optimization_saturation.pdf}
           {\tiny$G_b^i = (S_{b}^{i}-1)/(S_{b}^{*}-1)\in[0,1]$}
           \end{center}
    \end{column}
    \begin{column}{0.5\textwidth}
        \begin{center}
        \includegraphics[width=\textwidth, trim={0.25cm 0.25cm 0.25cm 0.25cm}, clip]{./images/fig19u_pass_impact_ranking_mean_median.pdf}
        {\scriptsize
            \begin{itemize}
                \item[\dingblue{43}] Pareto-skewed distribution: the top decile accounts for the majority of impact.
        
                \item[\dingblue{43}] The ranking is dominated by \texttt{early-cse}, \texttt{licm}, \texttt{instcombine}, \texttt{loop-vectorize}, \texttt{loop-rotate}, \texttt{loop-unroll}, \texttt{simplifycfg}, \texttt{sroa}, \texttt{indvars}.
            \end{itemize}
        }
        \end{center}
    \end{column}
    \end{columns}
\end{frame}

\begin{frame}[fragile]{\texttt{2mm} Linear-Algebra Kernel from PolyBench~\cite{Pouchet12}\footnote{
    \mylink{https://github.com/MatthiasJReisinger/PolyBenchC-4.2.1/blob/master/polybench.pdf}{github.com/MatthiasJReisinger/PolyBenchC-4.2.1/polybench.pdf}
}}
\begin{columns}[T]
	\begin{column}{0.5\textwidth}
        A double matrix-matrix multiplication (GEMM) in \(O(N^3)\) time.
		\begin{lstlisting}[basicstyle=\ttfamily\small, keywordstyle=\color{scribeaccent}\bfseries, language=C]
/* D = a*A*B*C + b*D */
for (i = 0; i < NI; i++)
 for (j = 0; j < NJ; j++) 
  for (k = 0; k < NK; k++)
   t[i][j] += a*A[i][k]*B[k][j];

for (i = 0; i < NI; i++)
 for (j = 0; j < NL; j++) {
  D[i][j] *= b;
  for (k = 0; k < NJ; k++)
   D[i][j] += t[i][k]*C[k][j];
 }
		\end{lstlisting}
	\end{column}
	\begin{column}{0.5\textwidth}
        Pratical utility of the kernel:
        \begin{itemize}
            \item[\dingblue{43}] \textbf{Data Reuse}: CPU/GPU spatial and temporal cache locality.
            \item[\dingblue{43}] \textbf{Pipeline Parallelism}: CPU/GPU SIMD, \textit{instruction level} parallelism (ILP), and superscalar pipelined execution.
            \item[\dingblue{43}] \textbf{Deep Learning}: the basic block during inference (and training) in neural networks is \texttt{2mm}.
            \item[\dingblue{43}] \textbf{Features}: \textit{affine} perfect (first) and imperfect (second) loop nests suitable for polyhedral optimizations.
        \end{itemize}
	\end{column}
\end{columns}
\end{frame}

\begin{frame}[fragile]{HW-counter signatures at the top-10 passes (\texttt{2mm})}
    \begin{columns}[T]
	\begin{column}{0.5\textwidth}
    \begin{center}
    {\scriptsize The blue-colored boxes are \textbf{improvements} over the baseline (\texttt{-O0}), while red-colored are \textbf{regressions}.}
    \includegraphics[width=\textwidth, trim={0.25cm 0.25cm 0.25cm 0.25cm}, clip]{./images/2mm.png}
    \end{center}
    \end{column}

    \begin{column}{0.5\textwidth}
        \begin{center}
        \begin{itemize}
            \item[\dingblue{43}] The branch predictor is not waiting: \texttt{branch\_misses} are 0 everywhere! Thanks to prefetching due to constant stride.
            \item[\dingblue{43}] \texttt{licm} is introducing lots of regressions, but recovered later.
            \item[\dingblue{43}] \texttt{loop-unroll} decreased \texttt{d1}, \texttt{l1\_i}, and \texttt{l1\_d} counters. The energy consumption is also decreasing.
            \item[\dingblue{43}] \texttt{loop-vectorize} is not soo significant, but it is still worth mentioning.
        \end{itemize}
        \end{center}
    \end{column}
    \end{columns}
\end{frame}

\begin{frame}[fragile]{HW counters aggregated across the PolyBench suite}
    \includegraphics[width=\textwidth, trim={0.25cm 0.25cm 0.25cm 0.25cm}, clip]{./images/fig16e_cache_ipc_cpi_energy_ribbon_2x3.pdf}
\end{frame}


\begin{frame}[fragile]{Bounded phase-interference loss}
    \begin{columns}[T]
	\begin{column}{0.5\textwidth}
    \begin{center}
    \includegraphics[width=0.9\textwidth, trim={0.25cm 0.25cm 0.25cm 0.25cm}, clip]{./images/potential_loss_phase_interference_left.pdf}
    \end{center}
    \end{column}

    \begin{column}{0.5\textwidth}
        \begin{center}
We \textbf{define} a novel optimistic additive bound:
\[
    S_\mathrm{opt} = \sum_i \max(\Delta S_i, 0),
\]
\[
    L = \frac{S_\mathrm{opt} - [S_\mathrm{actual}]_0^{S_\mathrm{opt}}}{S_\mathrm{opt}},
\]

where $[x]_0^{S_\mathrm{opt}} = \min(\max(x,0), S_\mathrm{opt})$ and $S_\mathrm{actual} = S_\mathrm{final} - 1$.

        \end{center}
    \end{column}
    \end{columns}



\end{frame}

\begin{frame}[plain,noframenumbering]
	\centering
	\vfill
	{\Large\bfseries Thank you!}
	\scribedivider
	\href{https://federicobruzzone.github.io/}{\texttt{federicobruzzone.github.io}}
	\vfill
\end{frame}

% ============================
% ========== Refs ============
% ============================
\begin{frame}[allowframebreaks, noframenumbering, plain]{References}
	\scriptsize
	\bibliographystyle{unsrt}
	\bibliography{local,strings,metrics,compilers,data_structures,programming,software_engineering,software_architecture,dsl,pl,splc,oolanguages,my_work,grammars,ml+nn,security,roles,learning,cop,testing,dsu,distributed_systems,reflection,aosd,foundations,petri-nets,pattern,logic}
\end{frame}

\end{document}
